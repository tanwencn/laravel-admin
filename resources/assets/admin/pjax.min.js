$.pjax.defaults.timeout=5000;$.pjax.defaults.maxCacheLength=0;$(function(){$(document).on("pjax:start",function(){NProgress.start()});$(document).on("pjax:end",function(){NProgress.done()});$(document).pjax("a[target!=_blank]","#pjax-container");$(document).on("pjax:timeout",function(a){a.preventDefault();toastr.warning(timeout_load);NProgress.done()});$(document).on("submit","form",function(a){$.pjax.submit(a,"#pjax-container")});$(document).on("pjax:send",function(a){if(a.relatedTarget&&a.relatedTarget.tagName&&a.relatedTarget.tagName.toLowerCase()==="form"){$submit_btn=$('form [type="submit"]');if($submit_btn){$submit_btn.attr("disabled",true)}}NProgress.start()});$(document).on("pjax:complete",function(a){if(a.relatedTarget&&a.relatedTarget.tagName&&a.relatedTarget.tagName.toLowerCase()==="form"){$submit_btn=$('form [type="submit"]');if($submit_btn){$submit_btn.removeAttr("disabled")}}NProgress.done()});NProgress.done()});